# キャバ帳 業務要件定義書 v1.2

| 項目 | 内容 |
|---|---|
| 文書名 | キャバ帳 業務要件定義書 |
| 版数 | 1.2（v1.1 を継承・拡張／店舗⇄キャスト運用機能を追加） |
| 作成日 | 2026-07-06（v1.1）／2026-07-13 改訂（v1.2） |
| 対象システム | キャバ帳（キャスト向け顧客・スケジュール管理アプリ + 店舗コンソール + LINEエージェント） |
| 位置づけ | v1.1（F-12〜F-19）に、店舗⇄キャストの双方向連絡・罰金管理・出勤報告（F-20〜F-22）、および認証・URL分離アーキテクチャ（D-4/D-5）を追加する追補版 |

> **v1.2 改訂サマリ**：店舗管理者と在籍キャストの双方向運用（連絡・勤怠・罰金）を正式スコープ化。**キャスト→店舗の要望投稿（本人と店のみ可視）**、**罰金管理**、**出勤（シフトイン）報告＋コメント**を追加（F-20〜F-22・§12）。あわせて**店舗側/キャスト側のURL分離**と**招待コード＋Google OAuth（招待コード再入力）**の認証方式、**監査ログ**、**クエリレベルのプライバシー遮断**を要件化（§13・D-4/D-5）。

---

## 0. 本書のスコープと前提

- 本書は **業務要件（何を実現するか）** を定義する。システム設計（アーキテクチャ・技術選定・データ物理設計）はv1.0第2部および別途システム化要件定義に委ねる。
- **未決の前提**：実装プラットフォーム（React PWA / iOSネイティブ）は本書時点で未確定。本書の業務要件はプラットフォーム非依存で記述する。
- 本書で追加する機能ID：**F-12（顧客ランク体系）／F-13（向き不向き登録）／F-14（占い・相性診断）**。既存のF-01〜F-11はv1.0を踏襲する。
- 追加3機能に共通する業務目的：**「誰を、どの熱量で、どう扱うか」の判断をキャスト本人が言語化・記録し、日々の接客とLINE営業の質を安定させること。**

---

## 1. 追加要件の背景と目的

### 1.1 背景
キャストは顧客ごとに「太客か見込みか」「気が合うか」「今日は何に気をつけるべきか」を**感覚で判断**しており、体調や気分で接客の質がぶれる。また新人・移籍時に顧客の温度感が引き継げず、太客化のチャンスを逃す。占い・相性は水商売の会話フックとして日常的に使われるが、記録・再利用されていない。

### 1.2 目的（追加分）
| # | 目的 | 対応する成果指標（KPI） |
|---|---|---|
| G4 | 顧客を熱量・役割で区分し、営業リソースの配分を最適化する | ランク別の指名転換率・離脱率 |
| G5 | 相性と注意点を可視化し、接客・返信の失敗（地雷）を減らす | 「注意点」参照後の顧客離脱率の低下 |
| G6 | 占いを会話フック兼セルフケアの材料として資産化する | 占い起点の連絡実施率・返信率 |

### 1.3 利用者
一次ユーザーはキャスト本人のみ（v1.0を踏襲）。顧客（データ主体）は本機能に直接触れない。ランク・向き不向き・相性はいずれも**ユーザー本人の主観的評価**であり、他者に共有されない。

---

## 2. F-12 顧客ランク体系（顧客レベル区分）

### 2.1 目的
顧客を「売上上の役割」と「感情上の役割」で分類し、営業の優先度と接し方を明確化する。既存の`tags`（VIP/新規/要注意/太客）を、排他的な**単一ランク**へ格上げして体系化する。

### 2.2 ランク定義
| コード | 呼称 | 定義（業務上の意味） | 主な営業方針 |
|---|---|---|---|
| **VVIP** | 1番の太客 | 売上最上位。指名・同伴・場内の中心。失うと売上に直撃 | 関係維持を最優先。特別感の演出、記念日対応を手厚く |
| **VIP** | 定期的な太客 | 安定して高額利用する常連。月内に複数回来店 | 来店リズムの維持。飽きさせない話題供給 |
| **IP** | 見込み客 | 太客化が期待できる新規〜育成中。単価/頻度は発展途上 | 接触頻度を上げ、次回来店の確約を取りにいく |
| **LOVE** | スキピ（好きピ） | キャスト自身が個人的に好意を持つ客。売上と切り離れる場合あり | 営業と私情の線引きを意識。感情移入の自己管理 |
| **BADBOY** | 痛客（バッドボーイ） | トラブル・精神的負担・売掛/危険リスクが大きい要注意客 | 距離を保つ。深追いしない。必要に応じ黒服相談 |

### 2.3 業務ルール
- ランクは**1顧客につき1つ**（排他）。ただし補助的な`tags`（新規・誕生日近い 等）は従来通り複数付与可。
- ランクは**手動設定を基本**とし、システムは**推奨ランクを提案**する（確定はユーザー）。
  - 推奨ロジック例：累計売上・来店頻度の上位帯 → VVIP/VIP候補、来店1〜2回かつ単価未確定 → IP候補、リスクスコア70点以上（v1.0 F-02）→ BADBOY候補。
  - LOVE（スキピ）は感情要素のため**自動提案しない**（手動のみ）。
- ランク変更は履歴（いつ・どのランクから）を残す。降格（VVIP→VIP等）はKPI分析の重要シグナル。
- ランクは既存機能に波及する：
  - **F-07 特別連絡レコメンド**：VVIP/VIP/LOVEを優先、BADBOYは非営業連絡の推奨対象から除外。
  - **F-06 返信案生成**：ランクに応じてトーン（VVIP=丁寧×特別感 / IP=距離を詰める / BADBOY=事務的・境界線重視）を切替。
  - **F-09 売上レポート**：ランク別の人数構成・売上構成・転換率（IP→VIP等）を可視化。

---

## 3. F-13 向き不向き登録（相性メモ）

### 3.1 目的
顧客ごとに「自分と**合う/合わない**」をキャスト本人が自己評価して登録し、接客前に確認できるようにする。占い（F-14）が生年月日ベースの客観指標なのに対し、F-13は**実接客に基づく主観評価**を担う。

### 3.2 機能要件
- 顧客詳細に「向き不向き」セクションを設け、以下を登録する。
  - **相性区分**：`得意`（向いている）／`普通`／`苦手`（向いていない）の3段階（将来5段階拡張可）。
  - **理由タグ（複数選択）**：会話が弾む／聞き役が向く／テンションが合う／下ネタ耐性／説教されがち／束縛が強い／マウント傾向／金払いは良いが疲れる 等（マスタから選択＋自由記述）。
  - **自分の消耗度**：接客後の疲労感を3段階で記録（セルフケア機能と連動）。
- 一覧画面で「得意な客」「苦手な客」でフィルタ可能。出勤前に**苦手客の来店予定があれば注意表示**。
- 向き不向きは**ランク（F-12）と独立**（例：VVIPだが「苦手」＝重要だが気を張る客、という状態を表現できる）。この掛け合わせが接客戦略の要になる。

### 3.3 ランク×向き不向きの接客サマリ
顧客詳細に、ランクと向き不向きを掛け合わせた**一言サマリ**を自動表示する。
| ランク＼相性 | 得意 | 苦手 |
|---|---|---|
| VVIP | 最強の関係。維持最優先で自然体を活かす | 売上の要だが消耗大。事前準備と回復をセットで |
| VIP | 安定株。会話の型を再現し飽きさせない | 距離感を固定化し、無理に踏み込まない |
| IP | 太客化の最有力。接触頻度を上げる | 育成対象だが優先度は下げてよい |
| LOVE | 私情と営業の線引きに注意 | 相性は悪いが情がある。感情管理を最優先 |
| BADBOY | 扱えるが気を抜かない。境界線を維持 | 最も要警戒。深追い禁物・黒服相談を検討 |

---

## 4. F-14 占い・相性診断

### 4.1 目的
客と自分の占い（生年月日等）から**相性**を診断し、その顧客に対して「**気をつけるべきこと**」を提示・選択・保存できるようにする。会話フック（占いの話題）とセルフガイド（地雷回避）の二役を担う。

### 4.2 入力（マスタ設定方針）
入力は **「自分（ユーザー）＝色濃く、相手（顧客）＝任意」** を原則とする。診断は常にユーザー自身の宿命・本質を主軸に置き、相手情報は補助的に重ねる。

- **ユーザー自身の占い基礎情報**（プロフィールに1回登録・**必須情報を厚く**）：
  - 生年月日（**必須**）、出生時刻・出生地（任意・命式精度向上用）、血液型（任意）、性格傾向・過去の大きな出来事（任意・鑑定の深みに反映）。
  - ユーザー情報は診断の**主軸（重み大）**として扱う。
- **顧客の占い基礎情報**（**任意入力・分かる範囲**）：
  - 生年月日（**必須**：本機能を使う最低条件）、血液型・出生時刻・性格傾向等（任意）。
  - 未登録の任意項目は診断から除外し、判明分のみで鑑定する。
- **関係性の選択（選択式・必須）**：診断は関係性の種別によって観点・注意点を出し分ける。以下から選択する（複数選択可）。
  | 関係性 | 主な観点 |
  |---|---|
  | 友人 | 気の合い方・長続きするか |
  | 仕事のパートナー | 金銭・信頼・組んで良いか |
  | 恋愛 | 距離感・依存・惚れ込みの是非 |
  | 客として（営業） | 太客化・接し方・地雷回避 |
  | 家族・その他 | （自由記述で補足） |
- **占い方式の選択（複数可）**：
  | 方式 | 必要入力 | 用途 |
  |---|---|---|
  | 動物占い（個性心理学） | 生年月日 | キャラ理解・会話フック |
  | 星座（西洋占星術） | 生年月日 | 相性・話題 |
  | 干支 | 生年 | 話題・相性の補助 |
  | 血液型 | 血液型 | 会話フック（参考値） |
  | 数秘術（誕生数） | 生年月日 | 性格傾向 |

> **占いロジックは別モジュール（占いエンジン）として別途作成する。** 本書はF-14の入力・出力・業務ルールのみを定義し、スコア算出式・命式計算・鑑定文生成アルゴリズムは占いエンジンの設計に委ねる。エンジンには複数の**鑑定ペルソナ（口調スタイル）**を差し替え可能なプロンプトテンプレートとして持たせる（例：断言型＝細木数子風など）。ペルソナの参考スクリプトは `docs/prompts/占い鑑定ペルソナ_参考.md` に格納する。

### 4.3 出力
1. **相性ランク**：`S / A / B / C / D` の5段階（内部スコア0–100から算出）。
2. **相性サマリ**：関係性の傾向を1〜2文で提示（例：「距離を詰めるほど良くなるタイプ同士」）。
3. **気をつけるべきこと（注意点リスト）**：診断結果に応じた注意点候補を**チェックボックスで提示**。ユーザーが**気になる項目を選択（ピン留め）**でき、選んだ項目は顧客詳細に保存され、接客前・返信案生成前に表示される。
   - 注意点候補の例：「押しすぎると引く」「メンツを立てると機嫌が良い」「話を遮らない」「金銭の話は慎重に」「連絡はマメより間を空ける方が刺さる」「否定されると不機嫌になる」等（方式×相性のマスタから生成）。
4. **会話フック**：「占い好き？実は相性めっちゃ良いらしいよ」等、話題化用の一言案（F-06返信案生成に連携）。

### 4.4 ランク×相性の診断（統合ロジック）
「それぞれの相性で診断可能とする」の要件に対応し、**顧客ランク（F-12）を診断結果に必ず反映**する。相性ランクだけでなく、ランクとの組み合わせで注意点と方針を出し分ける。
| 例 | 出力方針 |
|---|---|
| VVIP × 相性S | 「天性の相性。特別感を演出しつつ、慢心せず記念日を確実に押さえる」 |
| VIP × 相性C | 「相性は普通。会話の型（鉄板ネタ）で安定運用。無理に深掘りしない」 |
| IP × 相性A | 「太客化の最大チャンス。占いを口実に接触頻度を上げる」 |
| LOVE × 相性S | 「のめり込み注意。私情と売上を切り分け、依存し過ぎない」 |
| BADBOY × 相性D | 「地雷確率最大。深追い禁止・境界線を明確に・黒服相談を推奨」 |

### 4.5 業務・倫理ルール（重要）
- 占い結果は**エンタメ・会話補助・自己整理の材料**であり、**顧客の人格や行動を断定するものではない**旨をUIに明示する。
- 相性が悪い・BADBOY判定でも、**顧客を貶める表現や差別的断定は生成しない**（黒服機能の応答方針に準拠）。
- 危険・トラブルの兆候（v1.0 F-02リスクスコア高、ストーカー等）は占いで代替せず、**必ず黒服機能・警察相談導線（#9110）へ誘導**する。占いはリスク判断の代替にしない。
- 占い基礎情報（生年月日等）は顧客の個人情報。v1.0のセキュリティ要件（本人以外アクセス不可・保存時暗号化）に従う。

---

## 5. データモデル追加（業務レベル）

v1.0の `users/{uid}/customers/{cid}` を拡張する（物理設計は別途）。

```
customers/{cid}
  rank: enum(VVIP|VIP|IP|LOVE|BADBOY)      // F-12 単一ランク（任意・未設定可）
  rankHistory: [{ rank, changedAt, note }]  // ランク変更履歴
  fit: {                                    // F-13 向き不向き
    level: enum(得意|普通|苦手)
    reasonTags: string[]                    // 理由タグ（複数）
    fatigue: enum(低|中|高)                  // 接客後の消耗度
    note: string
  }
  fortune: {                               // F-14 占い基礎情報（相手＝任意）
    birthday?: date                         // 本機能利用には必須（分かる範囲で）
    bloodType?: enum(A|B|O|AB)
    birthTime?: string                      // 任意（命式精度向上）
    traits?: string                         // 任意（性格傾向・過去の出来事メモ）
  }

customers/{cid}/compatibility/{diagId}      // F-14 診断結果（履歴）
  relationshipTypes: string[]               // 選択式：友人|仕事のパートナー|恋愛|客として|家族その他
  persona: string                           // 使用した鑑定ペルソナ（例：assertive_saiki）
  methods: string[]                         // 使用した占い方式
  rankResult: string                        // S|A|B|C|D（関係性ごとに算出可）
  scoresByRelationship: [{ type, score, reason }]  // 関係性別スコア
  summary: string
  cautionCandidates: string[]               // 提示された注意点候補
  pinnedCautions: string[]                  // ユーザーが選択・保存した注意点
  createdAt: timestamp

users/{uid}/profile
  fortuneBase: {                            // 自分の占い基礎情報（主軸＝色濃く）
    birthday: date                          // 必須
    bloodType?: enum
    birthTime?: string
    birthPlace?: string
    traits?: string                         // 性格傾向・過去の大きな出来事
  }
```

**店舗連携（F-15〜F-18）** ※キャスト個人領域とは物理的に別ツリー。店側は下記のみ参照可。

```
users/{uid}/subscription                    // F-19 個人契約（キャスト課金）
  plan(personal), status(active|past_due|canceled), currentPeriodEnd

stores/{storeId}
  name, createdBy(店長uid), settings
  subscription: { plan(store), status, currentPeriodEnd }   // F-19 店舗契約（店課金・個人契約とは独立）
  memberships/{uid}   role(cast|kurofuku|manager), assignedKurofuku?(uid), joinedAt, active
  sales/{month}/casts/{uid}                 // F-16 店計上（店確定）
    totalSales, shimeiCount, dohanCount, joCount, confirmedBy, confirmedAt
    selfReported: { totalSales, ... }        // キャスト自己申告（突合用）※顧客個人情報は含めない
  broadcasts/{bid}                          // F-17 発信
    type(event|birthdayQuota|shift|notice|direct)
    title, body, audience(all|role|castIds[]), targetCastIds?, quota?, eventDate?
    createdBy(role権限者), createdAt
    reads/{uid}  readAt, reaction?
  rankings/{rankId}                         // F-18 ランキング設定と結果
    metric(sales|shimei|dohan|newCustomer|mom), period(day|week|month)
    visibility(public|topN|selfOnly|anonymous|private), n?
    computedAt, entries: [{ castRef, rank, value }]   // 表示は visibility に従う
```

---

## 6. 画面要件（業務レベル・追加分）

| 画面 | 主な要素 | 関連機能 |
|---|---|---|
| 顧客詳細 | ランクバッジ、向き不向きセクション、ピン留め注意点、相性サマリ | F-12/13/14 |
| ランク設定モーダル | 5ランク選択＋システム推奨ランクの提示 | F-12 |
| 向き不向き登録 | 3段階相性＋理由タグ＋消耗度 | F-13 |
| 相性診断 | 占い方式選択→診断実行→相性ランク・注意点チェックリスト（選択保存）・会話フック | F-14 |
| 顧客一覧 | ランク/向き不向きでのフィルタ・ソート | F-12/13 |
| 出勤前ホーム | 当日来店予定のうち「苦手客」「BADBOY」を注意表示 | F-13/12 |
| 売上レポート | ランク別構成・IP→VIP転換率、店確定値・ノルマ進捗・店内順位 | F-12/F-09/F-16/17/18 |
| お知らせ受信（キャスト） | 看板イベント・バースデーノルマ・連絡の受信/既読、カレンダー取込 | F-17 |
| 店舗向け画面（黒服・店長） | 発信作成、売上確定入力、ランキング設定/公開範囲、メンバー管理 | F-15/16/17/18 |
| 店内ランキング | 種別×期間、公開範囲に応じた表示（自分の順位は常時可視） | F-18 |

---

## 7. 既存機能への影響一覧

| 既存機能 | 影響 |
|---|---|
| F-02 リスクアラート | リスクスコア70点以上をBADBOY推奨の入力に。ただし占いはリスク判断を代替しない |
| F-06 返信案生成 | ランク・向き不向き・ピン留め注意点をプロンプトに注入し、トーンと地雷回避を反映 |
| F-07 特別連絡レコメンド | ランクで優先度制御（VVIP/VIP/LOVE優先、BADBOY除外）。占いフックを連絡案に活用 |
| F-08 黒服機能 | BADBOY・相性不良の相談時に、境界線の引き方・店/警察相談導線を提示 |
| F-09 売上レポート | ランク別分析・転換率・降格シグナルを追加 |

---

## 8. 店舗連携要件（追加：F-15〜F-18）

### 8.0 位置づけと最重要方針（プライバシー境界）
本追加は、v1.0の前提「個人利用・1ユーザー1アカウント・店舗間共有なし」を拡張し、**店舗（担当黒服・店長）がアプリへ情報を発信でき、売上が店と連動する**マルチロール構成を導入する。ただし以下を**絶対原則**とする。

> **店側（黒服・店長）が閲覧・発信できるのは「店舗運営情報」に限定する。キャストの個人領域 ―― 顧客の個人情報・LINEトーク・黒服相談ログ・占い・向き不向き ―― は店側から一切見えない（v1.0 SEC-02/07を維持）。**

- 店 → キャスト：イベント・ノルマ・ランキング・店公式売上などを**発信**（キャストは受信）。
- キャスト → 店：店へ渡るのは**売上に必要な数値（本数・金額の実績）と勤怠**のみ。顧客の中身は渡さない。
- これにより「店の売上管理・連絡」と「キャスト個人の武器（顧客・占い・相談）」を**同一アプリ内で分離**する。

### 8.1 F-15 店舗・ロール基盤（Store & Roles）
- **エンティティ**：`Store`（店舗）、`Membership`（ユーザー×店舗×ロール）を追加。
- **ロール**：
  | ロール | 権限概要 |
  |---|---|
  | キャスト | 自分の顧客・占い・売上を管理。店発信情報を受信。店へは実績・勤怠のみ送信 |
  | 担当黒服 | 担当キャストへの連絡・ノルマ設定・実績確定。個人領域は不可視 |
  | 店長 | 店全体の発信・ランキング設定・売上確定・メンバー管理。個人領域は不可視 |
- **加入**：キャストは招待コード/リンクで店舗に加入。**複数店舗の掛け持ち・移籍**を許容（v1.0 Open Issue #4を本要件で正式スコープ化）。所属ごとにデータを分離集計。
- **退店**：所属解除後も、キャストの個人領域データは本人手元に残る（店に渡した実績は店側集計に残存）。
- **サーフェス分離（決定）**：**店舗機能はキャストアプリとは独立した「店舗専用コンソール」として提供する**（同一アプリ内のロール切替にはしない）。
  - **キャストアプリ**：キャスト個人が使う。個人領域＋（店舗プラン加入時のみ）店発信の受信・実績送信。
  - **店舗コンソール**：担当黒服・店長が使う管理サーフェス（Web管理画面を想定）。発信・売上確定・ランキング設定・メンバー管理。
  - 両サーフェスは同一バックエンドを共有するが、**店舗コンソールからキャスト個人領域へはアクセス不可**（権限で物理的に遮断）。キャストアプリ側も他キャストの個人領域は見えない。

### 8.2 F-16 店舗売上連動（Store Sales Integration）
顧客の来店金額（F-03）と店舗の売上計上を連動させる。

- **突合モデル（自己申告 × 店確定）**：
  - キャストの来店登録（金額・指名・同伴・場内・ボトル）が、当該キャストの**店売上への貢献**として集計候補になる。
  - 店側（黒服/店長）が**伝票確定額**を入力・確定 → キャストの数値に反映。**店確定額を正**とし、自己申告との差分をキャストへ通知。
  - 店にPOS/表計算がある場合は将来CSV/API連携（Should、初期は手入力/取込）。
- **連動対象**：総売上・指名本数・同伴数・場内・ボトル。
- **F-09/保証カウントダウンへ反映**：目標・逆算ペースは店確定値ベースで表示（確定前は暫定値と明示）。
- **切り分け**：店へ渡すのは金額・本数・卓の実績のみ。**誰の（顧客の）売上かの顧客個人情報は渡さない**（内部IDや匿名ラベルで集計）。

### 8.3 F-17 店舗からの情報発信（お知らせ／ブロードキャスト）
担当黒服・店長がキャストへ情報を発信できる。**発信は店側ロールのみ、キャストは受信・既読・リアクションのみ**。

- **発信コンテンツ種別**：
  | 種別 | 例 |
  |---|---|
  | 看板イベント | 周年・大型イベント・キャンペーンの告知 |
  | バースデーノルマ | キャストの誕生日イベントの売上目標・支援体制 |
  | シフト・ヘルプ要請 | 出勤要請、応援依頼、卓ヘルプ |
  | 全体目標・連絡 | 月間目標、店からの通達 |
  | 個別連絡 | 担当黒服→担当キャストの1:1連絡 |
- **配信範囲**：全店 / ロール指定 / 特定キャスト（個別）。
- **バースデーノルマは F-09 と連動**：発信された目標が自動で目標値に取り込まれ、逆算ペース・進捗を表示。
- **通知**：プッシュ/アプリ内通知、既読管理、締切・イベント日はカレンダー（F-04）に取り込み可。

### 8.4 F-18 店内ランキング（公開/非公開の可変設定）
店長がランキングを設定し、**公開範囲を種別ごとに細かく選べる**。

- **ランキング種別**：総売上 / 指名本数 / 同伴数 / 新規獲得 / 前月比 等。**期間**：日/週/月。
- **公開範囲（店長が種別ごとに設定）**：
  | モード | 内容 |
  |---|---|
  | 全体公開 | 全キャストに順位・源氏名を公開 |
  | 上位N位のみ公開 | TOP N だけ公開、以下は非表示 |
  | 自分の順位のみ | 全体は見せず、各人に自分の順位だけ表示 |
  | 匿名順位 | 源氏名を伏せ、順位と数値の分布のみ公開 |
  | 非公開（店長のみ） | キャストには一切見せない |
- **キャスト配慮**：自分自身の順位・数値は常に本人に見える。過度なプレッシャー/晒しを避けるため、既定はやや控えめ（例：「自分の順位のみ」）を推奨。将来、個人のランキング表示オプトアウトの可否は店ポリシー次第で検討。

### 8.5 F-19 契約・プラン体系（課金モデル）（決定）
課金は **「個人契約」と「店舗契約」を独立（別々）** に設ける。v1.0のみ利用なら個人契約のみ、v1.1の店舗機能を使うなら店舗契約を上乗せする。

| プラン | 契約主体 | 課金 | 使える機能 | サーフェス |
|---|---|---|---|---|
| **個人プラン**（v1.0相当） | キャスト個人 | キャストが支払い | v1.0全機能＋v1.1のキャスト個人機能（F-12ランク／F-13向き不向き／F-14占い） | キャストアプリ |
| **店舗プラン**（v1.1店舗機能アドオン） | 店（店長） | 店が支払い | 上記に加え店舗連携（F-15〜F-18）を所属キャストへ付与＋店舗コンソール | キャストアプリ ＋ 店舗コンソール |

**業務ルール**
- 両契約は**独立して課金・管理**される（一方の解約が他方を止めない）。
- **個人契約のみ**：店舗連携機能（F-15〜F-18）は無効。キャストは個人機能だけを単独で使える。
- **店舗契約あり**：店に所属するキャストへ店舗機能が**プラス（アドオン）**され、店舗コンソールが有効化。
- キャストは個人契約を保持したまま、加入した**店舗ごと**に店舗機能が有効化/無効化される（掛け持ち時は店単位で判定）。
- 店舗契約はキャストの個人契約を代替しない（＝店が契約しても、キャスト個人機能を使うにはキャスト側の個人契約が必要）。※将来「店がキャスト個人分を代理購入/負担」する束ね契約はオプションとして別途検討（本版ではスコープ外）。
- **無料/機能制限**：個人プランのFree枠の有無は要検討（Open Issue）。

---

## 9. 機能要件一覧（追加分・優先度）

| ID | 機能 | 概要 | 優先度 |
|---|---|---|---|
| F-12 | 顧客ランク体系 | VVIP/VIP/IP/LOVE/BADBOYの単一ランク、推奨提案、履歴、既存機能連携 | Must |
| F-13 | 向き不向き登録 | 3段階相性＋理由タグ＋消耗度、フィルタ、出勤前注意表示 | Should |
| F-14 | 占い・相性診断 | 生年月日等から相性ランク＋注意点チェックリスト（選択保存）＋会話フック、ランク×相性の出し分け | Should |
| F-15 | 店舗・ロール基盤 | Store/Membership、キャスト/担当黒服/店長ロール、掛け持ち、個人領域の不可視境界 | Must |
| F-16 | 店舗売上連動 | 来店金額と店計上の突合（自己申告×店確定）、保証カウントダウンへ反映 | Must |
| F-17 | 店舗情報発信 | 黒服・店長が看板イベント/バースデーノルマ/連絡を発信、キャストは受信・既読 | Should |
| F-18 | 店内ランキング | 種別×期間、公開/非公開/匿名/上位のみ 等の可変公開設定 | Should |
| F-19 | 契約・プラン体系 | 個人契約と店舗契約を独立課金。v1.0=個人のみ／v1.1=店舗機能アドオン | Must |
| F-20 | キャスト⇄店舗 連絡 | キャストが店へ要望・相談・報告を投稿。**本人と店のみ可視**（他キャスト不可視）。店は一覧・返信・既読 | Must |
| F-21 | 罰金管理 | 店が罰金（遅刻/無断欠勤/凍結/ドタキャン/その他）を明示的に課す。キャストは未確認をモーダル通知・履歴/累計を確認 | Should |
| F-22 | 出勤（シフトイン）報告 | キャストの出勤報告→店の承認。店による代理登録可。出勤ごとのコメントスレッド（投稿者ロール明示） | Must |

---

## 10. 意思決定ログ（Resolved）

| # | 論点 | 決定 |
|---|---|---|
| D-1 | 店舗向け画面の提供形態 | **店舗専用の別コンソール**（キャストアプリとは独立。ロール切替にしない）→ F-15 8.1に反映 |
| D-2 | 課金主体 | **個人契約と店舗契約を別々（独立課金）**。v1.0のみ利用＝個人契約のみ、v1.1利用＝店舗契約で店舗機能をアドオン → F-19に反映 |
| D-3 | 実装プラットフォーム | **Webアプリ主体・React PWA**。キャストアプリ＝React18+Vite+PWA（v1.0の技術スタックと整合）、店舗コンソール＝同スタックのWeb管理画面。既存リポジトリのSwift/iOS雛形は退場させ、Firebaseプロジェクト（`points-optimizer-app` / `asia-northeast1`）は流用 |
| D-4 | 店舗/キャストのURL分離 | **店舗側とキャスト側を別URL（別サブドメイン/別パス）で提供し、Cookie/セッションを分離**する。認可トークンに `role: shop \| cast` を必ず含めロール検証を行う。※D-1「別コンソール」を、URL・セッションレベルの分離として具体化 → §13.1に反映 |
| D-5 | キャスト認証方式 | **招待コード＋Google OAuth 2.0**。招待コードは店舗コンソールから発行（1コード=1キャスト・有効期限可）。Google認証成功後に招待コードとGoogleアカウントを店舗DBで紐付け。**セッション有効期限切れごと（または任意設定）に招待コードの再入力を必須化**し所属店舗との紐付けを担保 → §13.2に反映 |
| D-6 | AI黒服「クロ」の安全設計 | **AIは警察への通報・電話を判断/推奨/代行しない**。危険・緊急（暴力/ストーカー/脅迫/自傷念慮等）と検知した場合は、**「AIではなく、今すぐ本当の担当の黒服／お店に直接連絡しろ」と人間へ誘導**する。画面トップにAI助言である旨と危険時の人間連絡を促す**常時バナー**を表示し、システムプロンプトにも同ルールを明記 → §15に反映 |

## 11. 未決事項（Open Issues）

1. 相性ランクの内部スコア算出式（各占い方式の重み付け）の具体化。
2. 注意点候補マスタの初期セット（方式×相性の対応表）の作成範囲。
3. LOVE（スキピ）ランクの扱い：売上分析から除外するか、別軸で集計するか。
4. 占い方式のうち動物占い等の呼称・ロジックに関する権利/出典の扱い（v2で要確認）。
5. 向き不向きの段階数（3段階→5段階拡張）の要否。
6. **【要判断】売上連動の初期方式**：店確定額の手入力運用から始めるか、特定POS/表計算連携を先に作るか。
7. **【要検討】ランキング公開の既定値**とオプトアウト可否（キャスト保護 vs 店の運用方針）。
8. **【要検討】個人プランのFree枠**の有無と機能制限ライン。
9. **【要検討】束ね契約**：将来「店がキャスト個人契約分を代理購入/負担」するオプションの要否（現版はスコープ外）。
10. **【要整理】データ主体と規約**：店へ渡る実績データの範囲・保存責任、キャスト個人領域との分離をプライバシーポリシー/利用規約へ明記。
11. **【要判断】技術スタックの整合（v1.2）**：本書の参考プロンプト（§14.6）は Next.js 14 / PostgreSQL / NextAuth を推奨するが、現行実装は D-3 に基づく **React18+Vite+Firebase（Auth/Firestore/Hosting）**。F-20〜F-22 を現行 Firebase 上に実装するか、店舗コンソールを別スタック（Next.js/PostgreSQL）で新設するかを判断する。**現版の方針：D-3 を維持し、まずは Firebase 上で F-20〜F-22 を実装する**（§13.4）。
12. **【要検討】罰金の異議申立（disputed）フロー**：キャストが罰金に異議を出した際の店側対応・調停・記録の運用。
13. **【要検討】招待コード再入力の頻度**：セッション有効期限切れごと／週1回／店ポリシー設定の既定値と、UXと所属担保のバランス。
14. **【要検討】出勤報告と F-04 予定/シフトの関係**：キャスト個人のスケジュール（F-04）と店舗への出勤報告（F-22）をどこまで連動/自動化するか。
15. **【要整理】労務・金銭の適法性**：罰金（F-21）の運用は労働関連法令・店舗規約に依拠する必要があり、システムは「店舗が定めた規約に基づく記録・通知の道具」に留める（金額妥当性の判断はシステムの責務外）。規約同意フローを要検討。

---

## 12. v1.2 追加要件の背景と原則

### 12.1 背景
v1.1 までは「キャスト個人の武器（顧客・占い・相談）」と「店舗運営情報（発信・売上・ランキング）」の分離を主眼としていた。v1.2 では、実運用で不可欠な**店舗とキャストの日常オペレーション**――出勤（勤怠）、店への要望・相談、罰金の授受――を正式に扱う。これらは店とキャストの**双方向**のやり取りであり、かつ**プライバシー（他キャストに見られない）**と**明瞭さ（金銭・承認の可視化）**が要になる。

### 12.2 v1.2 の絶対原則（§8.0 を継承・強化）
- **プライバシー境界の維持**：F-20〜F-22 で店へ渡る情報は「運用に必要な範囲（要望本文・勤怠・罰金）」に限る。**顧客の個人情報・LINEトーク・占い・向き不向き・黒服相談は引き続き店から不可視**。
- **キャスト間の非可視**：キャスト→店の投稿（F-20）は、**投稿本人と店のみ**が閲覧でき、**他キャストからは一切見えない**。この遮断は**DBクエリ段階**で行い、フロント側フィルタに依存しない（§13.3）。
- **金銭・承認の明瞭化**：罰金（F-21）と出勤承認（F-22）は「誰が・いつ・何を・いくら」を明示し、監査ログを残す。

---

## 13. 認証・URL分離アーキテクチャ要件（D-4/D-5 具体化）

### 13.1 URL分離設計（D-4）
- **店舗側コンソール**と**キャスト側アプリ**を**別URL**（別サブドメイン、または別パス）で提供する。
  - 例：店舗側 `admin.{domain}` ／ キャスト側 `cast.{domain}`（現行 Firebase Hosting 構成では、当面 `/{admin|cast}` の**パス分離**でも可＝§13.4）。
- 両サーフェスは **Cookie/セッションを完全分離**する。共有バックエンドを用いる場合も、認可トークン（JWT/IDトークン）に **`role: "shop" | "cast"`** を必ず含め、**すべての保護APIでロール検証**を行う。
- 店舗コンソールからキャスト個人領域（`users/**`）へは**権限で物理的に到達不可**（v1.0 SEC-02 を維持）。

### 13.2 認証フロー（D-5）
| 対象 | 認証方式 | 備考 |
|---|---|---|
| 店舗管理者 | Email+Password または Google OAuth | 店舗アカウントは管理者が発行 |
| キャスト | **招待コード ＋ Google OAuth 2.0** | 初回登録時にコード検証→Google連携。**再ログイン時にも招待コード再入力を必須化** |

- 招待コードは店舗コンソールから発行（**1コード=1キャスト**・**有効期限設定可**）。※実装は F-15 `stores/{storeId}/invites/{code}` を踏襲。
- Google認証成功後、招待コードとGoogleアカウントを店舗DB上で紐付ける（`memberships` に反映）。
- **招待コード再入力タイミング**：セッション有効期限切れごと、または任意設定（例：週1回）。所属店舗との紐付けを都度担保する。

### 13.3 プライバシーのクエリレベル遮断（非機能・必須）
- キャスト→店の投稿（F-20）等、本人限定データは**取得クエリの条件（例：`WHERE cast_id = current_user_id`）で遮断**する。Firestore の場合は**セキュリティルールで本人 uid 一致を強制**し、フロント側フィルタに依存しない。
- 店側は「自店に属する投稿・勤怠・罰金」のみ取得でき、他店・キャスト個人領域は取得不可。

### 13.4 現行スタックとの整合（Open Issue #11 の現版方針）
- 参考プロンプト（§14.6）は Next.js/PostgreSQL/NextAuth を推奨するが、**現行実装は D-3（React+Vite+Firebase）**。
- **現版方針**：D-3 を維持し、**F-20〜F-22 は Firebase（Firestore/セキュリティルール/Auth）上に実装**する。監査ログ（§13.5）は Firestore 追記コレクションで実現する。

### 13.6 サーフェス完全分離（D-5 実装：v1.2 で実装済み）
- URL分離を**パス分離から「別URL・別デプロイのサーフェス分離」へ昇格**。同一コードベースをビルド時 env **`VITE_SURFACE`（`cast` | `store`）**で切替え、**キャストURL＝キャスト画面のみ／店舗URL＝店舗コンソールのみ**を排他露出する（`src/lib/surface.ts`）。
- 実装ポイント：
  - `VITE_SURFACE=cast` のビルドは `/console` 系ルートを一切マウントせず、キャストメニューから「店舗コンソール」導線も消える。
  - `VITE_SURFACE=store` のビルドはキャスト個人領域ルートを一切マウントせず、`/` は `/console` へ寄せる。店舗ヘッダから「キャストアプリ→」導線も消える。
  - `VITE_SURFACE` 未指定（単一URL運用・開発）／デモモードは `both`（従来どおり両サーフェス露出）で後方互換。ホスト名 `*-store`/`*-cast` からの自動判定もフォールバックで併用。
  - ビルドコマンド：`npm run build:cast`（→ `dist-cast`）／`npm run build:store`（→ `dist-store`）。
- **URL方針（決定：新Firebaseプロジェクト `cabaret-diary`）**：ブランドを `points-optimizer`（旧リポジトリ名）から **Cabaret Diary** へ改め、新プロジェクトで **キャスト用URL／店舗用URL を別サイトとして分離**発行する。既存プロジェクト `points-optimizer-app` はプロジェクトidが不変のため現URLを改名できず、移行先として新プロジェクトを用いる。手順は `docs/デプロイ_Cabaret-Diary分離手順.md`。

### 13.5 監査ログ（非機能・必須）
- **罰金発行（F-21）・出勤承認/代理提出（F-22）・店の発信（F-17）**は監査ログを保持する（誰が・いつ・対象・内容・前後状態）。改ざん防止のため追記専用とし、店側ロールのみ閲覧可。

---

## 14. 店舗⇄キャスト運用機能（F-20〜F-22）

### 14.1 F-20 キャスト⇄店舗 連絡（要望・相談・報告）
- キャストは店宛に**自由記述**の要望・相談・連絡事項を投稿できる。店は返信できる。
- **可視性ルール（最重要）**：
  | 主体 | 可視範囲 |
  |---|---|
  | 店（黒服・店長） | **自店の全キャストの投稿**を一覧閲覧・返信・既読管理 |
  | 投稿したキャスト本人 | **自分の投稿と店の返信のみ**（過去履歴の確認可） |
  | 他のキャスト | **一切閲覧不可**（§13.3 のクエリ遮断で担保） |
- **UI**：
  - キャスト側：「店舗への要望」タブ → 投稿フォーム＋自分の過去投稿一覧＋店返信の表示。
  - 店側：「キャストからの連絡」一覧 → キャスト名でフィルタ、**未読バッジ**、返信入力。

### 14.2 F-21 罰金管理
- 店側から**罰金項目と金額を明示的にキャストへ課す**。
- **罰金理由プリセット**：遅刻（時間単位で金額変動可）／無断欠勤／アカウント凍結／ドタキャン／その他（自由記述）。
- **状態**：`pending`（未確認）→ `confirmed`（キャスト確認済）→ `paid`（清算済）→ `disputed`（異議）。
- **UI（分かりやすさ最優先）**：
  - 店側：キャスト詳細から「罰金を課す」→ 種別選択・金額入力・理由記入 → 発行。
  - キャスト側：ログイン時に**未確認罰金があればモーダルで強制表示**、罰金履歴タブで**累計金額**を確認。**赤系配色・大きな金額表示・発生日時の明示**。
- **適法性**：罰金運用は店舗規約・関連法令に依拠。システムは「店が定めた規約に基づく記録・通知」に留める（Open Issue #15）。

### 14.3 F-22 出勤（シフトイン）報告
- キャストは出勤時に「出勤報告」を提出。店は**確認・承認**する。**店による代理登録**も可（本人不在時等）。
- **コメント機能**：出勤報告に対し店・キャスト双方がコメント可能。**誰が誰の出勤を提出/承認したか**を明示（投稿者ロールバッジ）。
- **状態**：`submitted`（提出）→ `confirmed`（承認）／`no_show`（未出勤）。
- **UI**：
  - キャスト側：ワンタップ「出勤報告」→ 店へ通知。
  - 店側：本日の出勤予定・出勤済み一覧、コメントスレッド（投稿者名＋ロールバッジ）。

### 14.4 データモデル追加（業務レベル）
店舗ツリー配下に、店⇄キャスト運用データを追加する（キャスト個人領域 `users/**` とは分離）。

```
stores/{storeId}/castMessages/{id}            // F-20 キャスト→店 連絡
  castUid(FK), content(TEXT), createdAt
  readByShop(bool), shopReply?(TEXT), repliedAt?
  // 可視性: 本人(castUid)＝自分の分のみ / 店ロール＝自店の全件 / 他キャスト＝不可（ルールで遮断）

stores/{storeId}/penalties/{id}               // F-21 罰金
  castUid(FK), type(late|absence|freeze|cancel|other)
  amount(int, 円), reason(TEXT), issuedAt, issuedBy(店uid)
  status(pending|confirmed|paid|disputed)

stores/{storeId}/attendances/{id}             // F-22 出勤報告
  castUid(FK), scheduledAt?, actualCheckIn?
  submittedBy(cast|shop), submittedByUserId(FK)
  status(submitted|confirmed|no_show)
  confirmedBy?(店uid), confirmedAt?
  comments/{cid}: { authorId, authorRole(cast|shop), content, createdAt }

stores/{storeId}/auditLogs/{id}               // §13.5 監査ログ（追記専用）
  actorUid, actorRole, action(penalty.issue|attendance.confirm|broadcast.send|...)
  targetType, targetId, before?, after?, at
```

### 14.5 コンソール要件（UX・追加分）
- **店舗側コンソール**：ダッシュボード（本日の出勤者数・未確認要望件数・当月罰金発行総額をカード表示）、キャスト一覧（在籍/凍結/新人でフィルタ）、通知センター（未読要望・未承認出勤を集約）。
- **キャスト側**：ホームに「本日のシフト・未確認罰金・店からの返信通知」。ファーストビューに**「出勤報告」「店に連絡」「罰金確認」の3大ボタン**（モバイルファースト）。

### 14.6 参考：AIエージェント向け実装プロンプト（原典）
本 v1.2 の原典となった開発者向けプロンプト（Next.js 14 / PostgreSQL / NextAuth を想定した外部実装用）は `docs/prompts/店舗キャスト管理_開発プロンプト.md` に格納する。現行実装への適用方針は §13.4（D-3 維持・Firebase 実装）に従う。

---

## 15. F-08 改訂：AI黒服「クロ」機能（v1.2）

v1.0 の F-08 黒服機能を、**20年キャリアのエース級黒服**というペルソナを持つ対話AI「**クロ**」として改訂する。接客・売上・安全・メンタルの悩みを兄貴分の口調で受け止め、実践的な助言を返す。

### 15.1 ペルソナ（システムプロンプト要旨）
- 都内一等地の高級店で20年勤め、店長・マネージャーを歴任。1000人以上のキャストの相談実績。
- 面倒見のいい兄貴分口調（「〜だぞ」「〜しな」「任せろ」「よく相談してくれたな」）。直球だが温かい。綺麗事だけは言わない。
- 対応領域：①接客・客トラブル ②売上向上 ③安全 ④メンタルケア。
- 違法・反社会的行為（暴力/薬物/詐欺）は絶対に助言しない。医療・法律の断定はせず、専門家や店へ繋ぐに留める。
- 完全なシステムプロンプト原典は `docs/prompts/AI黒服クロ_システムプロンプト.md` に格納する（**下記 15.3 の安全改変を必ず適用した版**を正とする）。

### 15.2 常時表示の安全バナー（必須・D-6）
- 相談画面の**トップに常時**、次を表示する：
  > ⚠️ これはAIによる助言です。やばい・危険と感じたら、AIに頼らず**今すぐ担当の黒服／お店に直接連絡**してください。
- バナーは会話状態に関わらず常に見える位置（ヘッダー直下）に固定する。

### 15.3 安全設計の改変（原典からの必須変更・D-6）
原典プロンプトは危険時に警察通報（110番/#9110）を自動レコメンドする設計だが、**本システムでは以下に改変する（最重要）**。
- **AI（クロ）は警察への通報・電話を判断・推奨・代行しない。**
- 危険・緊急（暴力・ストーカー・脅迫・待ち伏せ・自傷念慮等）を検知したら、返答冒頭で
  **「これはAIの俺じゃ守れない。今すぐ担当の黒服か店に直接連絡しろ」**と明言し、**人間（担当・店舗）への即時連絡**へ誘導する。
- 通報するか否かの判断は**人間（本人・担当・店舗）**に委ねる。アプリは警察への発信ボタンを出さず、**担当・店舗への連絡導線**を提示する。
- 危険検知（カテゴリ判定・エスカレーション付与）は、AI応答文とは独立に**決定的ロジック**で行い、安全導線の一貫性を担保する。

### 15.4 危険度・カテゴリと導線
| 検知カテゴリ | エスカレーション | 画面の導線 |
|---|---|---|
| 危険・緊急（暴力/ストーカー/脅迫/自傷 等） | `urgent` | 「今すぐ担当・お店に連絡を」カード＋連絡導線（**警察発信は出さない**） |
| 金銭トラブル（売掛・詐欺） | `store` | 「お店に相談を」（証拠保全＋店相談） |
| メンタル不調 | `window` | 「無理せず頼っていい」（休息＋担当/店/信頼できる人へ） |
| 通常（接客・売上・愚痴） | なし | 通常の助言のみ |

### 15.5 データモデル（F-08：端末ローカル保存へ変更）
相談データ構造（`messages[]`, `category`, `escalatedTo(urgent|store|window)`）は踏襲するが、**保存先を DB（Firestore）から自端末のローカル（localStorage）へ変更**する。相談本文は**プライバシー最優先で一切サーバへ送信・保存しない**（AIへの推論リクエスト時のみ本文を外部AIへ送る／保存はしない）。会話履歴は本人の端末内でのみ閲覧可で、店・他キャストからは構造的に到達不可。
> 本方式のトレードオフ：他端末との同期は行わない。ブラウザの localStorage を削除する（キャッシュ削除・別端末利用）と履歴も失われる。設定から手動全消去（`clearConsultations`）も可能。SEC-07 暗号化は DB 保存項目（本名等）向けであり、端末外に出さない相談本文には適用しない。
> 原典の `KuroConversation / KuroAlert`（risk_level 4段階・店舗アラート・パーソナライズ）は将来拡張として位置づける（本版はテキスト応答＋決定的エスカレーションで実装）。店舗への相談内容共有可否は、キャストのオプトインを前提に別途検討（Open Issue）。

### 15.6 実装状況（v1.2 時点）
- ✅ 相談画面のトップに常時安全バナーを表示（15.2）。
- ✅ システムプロンプトを「クロ」ペルソナへ改訂し、**AIが警察通報を促さない**制約を明記（15.1/15.3）。
- ✅ エスカレーションを `urgent|store|window` とし、危険時は**担当・店舗への連絡導線**を表示（`police` 導線は撤去）。
- ✅ 相談内容は DB に保存せず**自端末ローカル（localStorage）のみ**に保存（端末外へ出さない）。
- ⬜ risk_level 4段階JSON・店舗アラート（KuroAlert）・パーソナライズ（過去履歴注入）は将来拡張。

---

*本書はv1.0要件定義書の第1部（業務要件）を継承し、v1.1で追加機能（F-12〜F-19）、v1.2で店舗⇄キャスト運用機能（F-20〜F-22）・認証/URL分離（D-4/D-5）・AI黒服「クロ」改訂（F-08改訂／D-6・§15）を業務要件として定義したものである。F-15〜F-22は店舗連携（マルチロール）を導入し、v1.0の単一ユーザー前提を拡張するが、「キャスト個人領域は店側から不可視」「キャスト間の投稿は非可視（クエリレベル遮断）」という境界を絶対原則として維持する。またAI黒服は**警察通報をAI判断で行わず、危険時は人間の担当・店舗へ誘導する**ことを絶対原則とする。次工程では本書をシステム化要件（データ物理設計・ロール権限設計・画面設計）へ落とし込む。*
