# キャバ帳 UI/UX 自己レビュー 100件（改修可否つき）

対象: 現行 React 実装（https://points-optimizer-app.web.app ・デモ: /demo ）
凡例（改修可否）:
- ✅ **可(低)** … 小さな変更で対応可。すぐ入れられる
- 🟡 **可(中)** … 実装は可能だが中規模／ライブラリ追加や要確認
- 🔴 **大/要判断** … 大規模・事業判断・基盤が必要
- ⚪ **見送り/任意** … 現状で妥当。好み・優先度低

---

## A. デザインシステム・全体
1. 単一バンドル754KB(>500KB警告)。初回表示が重い → コード分割 … 🟡
2. ダークモードがOS追従のみ。アプリ内トグルが無い … 🟡
3. フォント(Zen Old Mincho/Noto Sans JP)がGoogle Fonts CDN依存。オフラインで未適用の恐れ → self-host … 🟡
4. カラートークンがconfig＋各所ハードコード(#e6789b等)に散在 → tokens集約 … 🟡
5. `rose`追加済みだが`#e6789b`直書きが残存。統一余地 … ✅
6. radius/shadowのスケール未定義(xl/2xl/full混在) … ✅
7. アイコンが絵文字依存。端末で描画差・トーン不統一 → SVG化 … 🟡
8. 一部カスタムボタンでフォーカス可視性が弱い … ✅
9. トーストが1件ずつ上書き。キュー化で複数対応 … ✅
10. Empty状態が文字のみ(イラスト無し) … 🟡
11. ローディングが文字のみ。スケルトン化で体感向上 … 🟡
12. 金額/日付のtabular-numsが一部のみで桁ズレ … ✅

## B. ナビゲーション・レイアウト
13. サブ画面→ルートへの戻りが「戻る」頼み(タブ非表示のため) … ⚪
14. 画面遷移アニメが実ルーターでは無い(モック用のみ) … 🟡
15. 遷移でスクロール位置が保持されない … 🟡
16. 横画面レイアウト未最適化 … 🟡
17. iPhone SE(375px)でのピル/グリッド詰まり検証要 … ✅
18. env(left/right)のsafe-areaが一部未適用 … ✅
19. 戻るラベルが固定「‹ 戻る」。文脈化余地 … ⚪
20. 来店シート表示中に背景がスクロール可能(body scroll lock無し) … ✅
21. キーボード表示時に入力欄が隠れる恐れ(visualViewport未対応) … 🟡
22. PWAインストール(A2HS)案内が無い … 🟡
23. オフライン時のフォールバック/通知が無い … 🟡
24. 未定義パスは全て`/`へ。404画面が無い … ⚪

## C. ログイン
25. 背景がグラデのみで装飾控えめ … ⚪
26. ログイン手段がGoogleのみ(メール/Apple無し) … 🔴
27. エラー文言が汎用的。原因別ガイドが親切 … ✅
28. ボタンにGoogleロゴが無くブランドガイドライン非準拠の恐れ … ✅
29. 利用規約/プライバシーへのリンクが無い(文言のみ) … ✅(要ページ)
30. loading中にnull→一瞬白画面。スプラッシュが親切 … ✅

## D. ホーム
31. 源氏名未設定時「こんばんは🌙」で設定導線が弱い … ✅
32. 「今日の予定」が当日のみ。早朝跨ぎを拾えない場合 … ✅
33. 要確認顧客が上限なし表示でホームが長くなる → 上位N＋全件導線 … ✅
34. クイックタイルが2つのみ。占い/お知らせ導線が深い … ⚪
35. ひとこと連絡が生成前は空カードで価値が伝わりにくい … ✅
36. 太客TOPが売上順のみ(指名/来店切替無し) … 🟡
37. ホームに当月売上サマリ(保証カウントダウン)が無い … ✅
38. リスクスコアの凡例(何が良い/悪い)が無い … ✅
39. ヘッダーにブランドロゴが無い … ⚪
40. Pull-to-Refreshが無い(購読型なので必須でない) … ⚪
41. 通知ベル/未読バッジが無い(お知らせ更新が気付けない) … 🟡
42. 挨拶が時間帯固定「こんばんは」。時刻で出し分け … ✅

## E. 顧客一覧
43. カナ/ヨミ検索不可(ヨミ項目が無い) … 🟡
44. 五十音インデックスが無い(大量時に探しにくい) … 🔴(ヨミ必要)
45. 並び替えに「名前順/最終来店順」が無い … ✅
46. ランクフィルタが単一選択(複数不可) … ✅
47. 行のタグ表示が1つのみ(複数見えない) … ✅
48. お気に入り/ピン留めが無い … 🟡
49. スワイプのクイック操作(発信/削除)が無い … 🟡
50. ページング/仮想化無し。多件数で重い … 🟡
51. 検索0件時にフィルタ解除導線が無い … ✅
52. アバターがイニシャルのみ(画像非対応) … 🟡(Storage)

## F. 顧客詳細
53. 電話番号がコピーのみ。tel:発信リンクが無い … ✅
54. LINEを開くdeeplink(line://)が無い … ✅
55. 誕生日が月日のみ。年齢/星座が出ない … ✅
56. リスクのセルフチェックが保存されない(表示のみ) … 🟡
57. 来店タイムラインが全件表示で長い(折り畳み無し) … ✅
58. 相性(fit)と占い結果の関連が見えにくい … ⚪
59. rankHistory(ランク変遷)が保存されるが表示が無い … ✅
60. メモが単一フィールド(日付き追記ログにできると営業日誌に) … 🟡
61. 削除にUndoが無い(誤削除リカバリ不可) … 🟡
62. ボトルキープ残量管理が無い … 🔴(新データ)
63. 同伴/アフター回数の集計サマリが無い … ✅
64. 写真添付UIが無い(photoRefsフィールドは存在) … 🟡(Storage)

## G. 顧客 登録/編集
65. あだ名以外バリデーション無し(電話形式等) … ✅
66. 誕生日入力が西暦必須。年齢/和暦補助が無い … ⚪
67. タグ/相性理由が読点区切りテキスト(重複/誤り容易) → チップ追加UI … 🟡
68. フォームが縦長でセクション折り畳みが無い … ✅
69. 保存が上部バーのみ(長文で上部まで戻る) … ⚪(下部併設と要トレードオフ)
70. 入力中離脱で未保存警告が無い … 🟡
71. 本名の暗号化 … ✅**対応済み**（SEC-07 パスフレーズE2E暗号化を実装。メニューで設定/解除）
72. 年収「不明」既定が未選択で空のまま … ⚪
73. キーボード表示時の対象入力へのscrollIntoViewが無い … 🟡
74. 金額系の桁区切り表示が無い … ✅
75. 会社名/職業のサジェスト無し … ⚪
76. 送信中スピナーが弱い(disabledのみ) … ✅

## H. 予定
77. 月/週カレンダーグリッドが無くリストのみ … 🔴(ライブラリ)
78. GCal未連携時の価値説明が弱い … ✅
79. 繰り返し予定(毎週出勤等)が無い … 🟡
80. appointmentで顧客を選べない(同伴/アフターのみ) … ✅
81. 終了<開始で自動+1日する仕様が非明示 … ✅
82. 予定削除UIが編集画面に無い可能性(作成/更新のみ) … 🟡
83. 過去の予定が見えない(当日/今後のみ) … ✅
84. リマインド通知が無い … 🔴(プッシュ基盤)

## I. 返信アシスト
85. 生成失敗時のローカルフォールバックが「local」注記のみで分かりにくい … ✅
86. 3トーン固定。長さ/絵文字量の調整UIが無い … 🟡
87. 選んで「送る」導線(LINE共有/Web Share)が無い … ✅
88. LINEトーク取込の手順が分かりにくい(ガイド無し) … ✅
89. 生成履歴が残らない … 🟡
90. 相手未入力でも取込から生成する挙動が非明示 … ✅

## J. 黒服相談
91. 安全導線(#9110/店/窓口)が「デモ」トースト止まりで実発信/リンク無し … 🟡
92. 単一スレッド前提。新規作成/一覧が無い(複数持てる設計) … 🟡
93. 送信中が「…」のみ(タイピング表示無し) … ✅
94. 相談本文の暗号化 … ✅**対応済み**（SEC-07。パスフレーズ設定時に暗号化保存・復号）
95. 既読/時刻表示が無い … ✅

## K. 占い・相性診断
96. 自分の誕生日は保存対応済みだが、変更する設定画面が無い … ✅
97. 関係別スコアが多いと結果が縦長 … ⚪
98. 過去診断の履歴一覧が無い … 🟡
99. 結果のシェア(画像/リンク)が無い … 🟡(Web Share)
100. ペルソナ(細木数子風)が固定で選べない … ⚪

---

## 集計（改修可否）
- ✅ 可(低): 約45件 … 小改修の積み上げで体験が大きく向上
- 🟡 可(中): 約35件 … ライブラリ/データ拡張が必要
- 🔴 大/要判断: 8件（#26 認証追加, #44 五十音, #62 ボトル, #71 本名暗号化, #77 カレンダー, #84 リマインド, #94 相談暗号化, ほか）
- ⚪ 見送り/任意: 約12件

## 特に優先すべき（私の推奨）
1. **セキュリティ2件(#71/#94)**: 「暗号化保存」と表示しながら平文保存は表示と実態の乖離。文言修正 or SEC-07実装のどちらかを早急に。**最優先で判断を仰ぎたい**。
2. **すぐ効く小改修パック**: #53 tel発信, #45 並び替え追加, #46/#47 タグ複数, #33 要確認上限, #37 ホーム売上サマリ, #85 生成元表示, #87 送る導線, #96 誕生日設定 … いずれも✅で低コスト。
3. **中規模で価値大**: #14 遷移アニメ, #22 PWAインストール, #56 セルフチェック保存, #98 占い履歴。
